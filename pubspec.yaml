name: maxin_task
description: a task for maxin
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  dio: ^4.0.6
  intl: ^0.17.0
  get_it: ^7.2.0
  provider: ^6.0.2
  google_fonts: ^2.2.0
  cupertino_icons: ^1.0.4
  percent_indicator: ^4.0.0
  json_serializable: ^6.1.6
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.7
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
  generate: true